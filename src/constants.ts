// Global constant values

// The number of available avatars
const NUM_AVATARS = 6;

// All the images used by tiles. Indicies correspond to enum values of
// TileType
const IMGS = [
    "5", "10", "25", "50", "100", "volcano", "tornado", "hurricane", "thief"
].map(x => `<img src="img/tile-${x}.png"></img>`);
